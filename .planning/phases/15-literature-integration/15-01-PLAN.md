---
phase: 15-literature-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/repo-to-paper-skill/SKILL.md
autonomous: true
requirements: [REPO-04]

must_haves:
  truths:
    - "After H2 confirmation, the Skill derives search queries from H1+H2 titles and collects references via Semantic Scholar MCP"
    - "Ref files are saved to {repo_path}/.paper-refs/ with per-H1-section organization (introduction.md, methods.md, etc.)"
    - "Each ref entry contains title, authors, year, citations, relevance, abstract summary, and BibTeX in Markdown card format"
    - "When Semantic Scholar MCP is unavailable at pre-flight, Step 2.5 is skipped entirely and [CITATION NEEDED] placeholders are inserted"
    - "User sees progress per H2 subsection and a summary table before confirming to proceed to H3"
  artifacts:
    - path: ".claude/skills/repo-to-paper-skill/SKILL.md"
      provides: "5-step workflow (1, 2, 3, 2.5, 4) with literature collection step"
      contains: "Step 2.5: Literature Collection"
  key_links:
    - from: ".claude/skills/repo-to-paper-skill/SKILL.md"
      to: "mcp__semantic-scholar__papers-search-basic"
      via: "Step 2.5 pre-flight check and batch search calls"
      pattern: "papers-search-basic"
    - from: ".claude/skills/repo-to-paper-skill/SKILL.md"
      to: "mcp__semantic-scholar__get-paper-abstract"
      via: "Step 2.5 abstract fetch for papers with empty abstracts"
      pattern: "get-paper-abstract"
    - from: ".claude/skills/repo-to-paper-skill/SKILL.md"
      to: "{repo_path}/.paper-refs/"
      via: "Step 2.5 ref file writing"
      pattern: "\\.paper-refs/"
---

<objective>
Add a Literature Collection step (Step 2.5) to the repo-to-paper-skill that automatically collects
academic references via Semantic Scholar MCP after H2 outline confirmation. References are saved as
per-section Markdown files in the target repo's `.paper-refs/` directory.

Purpose: Fulfill REPO-04 -- at H2 completion, all section references are collected with metadata,
abstracts, and BibTeX entries, ready for body generation in Phase 16.

Output: Modified `.claude/skills/repo-to-paper-skill/SKILL.md` with Step 2.5 integrated into the
existing 4-step workflow.
</objective>

<execution_context>
@C:/Users/Lylll/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lylll/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-literature-integration/15-CONTEXT.md
@.planning/phases/15-literature-integration/15-RESEARCH.md
@.planning/phases/14-repo-to-paper-core-structure/14-01-SUMMARY.md

<interfaces>
<!-- Key patterns the executor needs from the existing literature-skill MCP usage -->

From .claude/skills/literature-skill/SKILL.md (Step 1 pre-flight):
```markdown
1. Call `mcp__semantic-scholar__papers-search-basic` with `{"query": "test", "limit": 1}`.
2. If the call succeeds: proceed to Step 2.
3. If the call fails or tool is unavailable: refuse immediately
```

From .claude/skills/literature-skill/SKILL.md (Step 5 BibTeX):
```markdown
Citation key format: `firstAuthorLastnameLowercaseYYYYfirstKeyword` (e.g., `smith2023urban`).
Use `@article` for journal papers, `@inproceedings` for conference papers, `@misc` for preprints.
If any field (volume, pages, DOI) is not returned by MCP, omit that field.
```

From .claude/skills/repo-to-paper-skill/SKILL.md (current structure):
```
Step 1: Scan Repository          (lines 102-122)
Step 2: Generate H1 Outline      (lines 125-153)
Step 3: Generate H2 Outline      (lines 156-178)
Step 4: Generate H3 Outline      (lines 183-200)
Output Contract                   (lines 202-216)
Edge Cases                        (lines 218-228)
Fallbacks                         (lines 230-237)
Examples                          (lines 239-281)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 2.5 Literature Collection to repo-to-paper-skill</name>
  <files>.claude/skills/repo-to-paper-skill/SKILL.md</files>

  <read_first>
    - .claude/skills/repo-to-paper-skill/SKILL.md (the file being modified -- current 286-line 4-step workflow)
    - .claude/skills/literature-skill/SKILL.md (MCP pre-flight check pattern at Step 1, search call pattern at Step 3, BibTeX generation at Step 5)
    - .planning/phases/15-literature-integration/15-CONTEXT.md (all locked decisions -- ref file format, query strategy, volume targets, user review flow, fallback behavior)
    - .planning/phases/15-literature-integration/15-RESEARCH.md (architecture patterns, anti-patterns, code examples, pitfalls)
    - references/skill-conventions.md (line budget guidance -- CONTEXT.md explicitly allows exceeding 300 lines)
  </read_first>

  <action>
Modify `.claude/skills/repo-to-paper-skill/SKILL.md` with these specific changes:

**1. Frontmatter updates:**
- Add `External MCP` to the `tools` list (currently `[Read, Write, Structured Interaction]`, becomes `[Read, Write, Structured Interaction, External MCP]`)
- Update `description` to mention literature collection: append "Collects literature references via Semantic Scholar MCP at H2 stage." to the existing description (both English and Chinese lines)
- Add `literature_refs` to `output_contract` list (currently `[scan_summary, paper_outline]`, becomes `[scan_summary, paper_outline, literature_refs]`)

**2. Purpose section update:**
- Add one sentence at the end of the existing Purpose paragraph: "After H2 confirmation, the Skill automatically collects academic references via Semantic Scholar MCP and saves them as per-section ref files for downstream body generation."

**3. Modes table update:**
- No change needed -- guided mode covers Step 2.5 naturally

**4. Workflow section -- insert new Step 2.5 after Step 3 (H2 confirmation):**

Insert a new `### Step 2.5: Literature Collection` section between Step 3 and Step 4 with this exact content structure:

```markdown
### Step 2.5: Literature Collection

**Pre-flight:**
- Call `mcp__semantic-scholar__papers-search-basic` with `{"query": "test", "limit": 1}`
- If call succeeds: proceed to literature collection below
- If call fails or tool unavailable: skip Step 2.5 entirely
  - Insert `[CITATION NEEDED]` after each H2 subsection description in the outline
  - Display: "Semantic Scholar MCP not available. Skipping literature collection. [CITATION NEEDED] markers added."
  - Proceed directly to Step 4 (H3 generation)

**Collect references:**

FOR each H1 section:
  FOR each H2 subsection under this H1:
    1. Derive search query: extract 2-5 key technical terms from the H2 title and description,
       contextualized by the H1 section title. Use English terms only (ignore Chinese translations
       in bilingual mode). Strip filler words (and, of, the, for).
       Example: H1="Methods", H2="Gradient Boosting Prediction Framework",
       description="Feature engineering using street-level semantic segmentation data"
       -> query: "gradient boosting prediction street-level semantic segmentation"
    2. Call `mcp__semantic-scholar__papers-search-basic` with `{"query": derived_query, "limit": 10}`
    3. For each result where the abstract field is empty: call `mcp__semantic-scholar__get-paper-abstract`
       with the paper's `paperId`. If abstract is still empty after fetch, mark as "Abstract not available"
    4. Filter results by relevance: assess how many distinct claims/arguments the H2 subsection needs
       to make, then keep 5-10 papers that best support those claims. Discard papers that are only
       tangentially related based on title and abstract content
    5. Display progress line: `✓ 1.1 Research Background: 8 refs found` (or `⚠ 1.1 Research Background: 0 refs found` for zero results)
    6. If a search call fails mid-batch: mark that H2 as `[CITATION NEEDED]`, display warning, continue to next H2
  END FOR
END FOR

**Write ref files:**

Create `{repo_path}/.paper-refs/` directory. Write one Markdown file per H1 section, named by
the section topic in lowercase (e.g., `introduction.md`, `methods.md`, `results.md`).

Each file uses this structure:

```markdown
# [H1 Section Title] - References

## [H2 Number] [H2 Subsection Title]

### [FirstAuthor] et al. ([Year])
**Title:** [Full title from MCP]
**Authors:** [Author1; Author2; ...]
**Year:** [YYYY] | **Citations:** [N]
**Relevance:** [[H2 number] [H2 subsection title]]
[One-sentence explanation of why this paper is relevant to this subsection]

> [Abstract summary: 1-2 sentences from MCP abstract data. If abstract not available, write "Abstract not available"]

```bibtex
@article{[citationkey], ...}
```
```

BibTeX rules (reuse literature-skill patterns):
- Citation key format: `firstAuthorLastnameLowercaseYYYYfirstKeyword` (e.g., `smith2023urban`)
- Entry type: `@article` for journal, `@inproceedings` for conference, `@misc` for preprints (follow MCP paper type)
- All BibTeX fields MUST come from MCP-returned data. If a field is not in the MCP response, OMIT it
- If DOI missing, add comment: `% DOI not available -- verify manually`
- Never fill missing fields from prior knowledge

Duplicate handling: allow same paper to appear in multiple section files. Each file is self-contained.

**Display summary table:**

After all H2 subsections processed, display:

```
| Section | Subsection | Refs | Top Reference |
|---------|-----------|------|---------------|
| 1. Introduction | 1.1 Research Background | 8 | Smith et al. (2023) - 142 citations |
| ... | ... | ... | ... |

Total: [N] references collected. Confirm to proceed to H3 outline generation.
```

Wait for user confirmation before proceeding to Step 4.
```

**5. Output Contract section update:**
- Add a new row to the Output table:
  `| literature_refs | Per-section Markdown files in {repo_path}/.paper-refs/ with reference cards | After Step 2.5 (skipped if MCP unavailable) |`

**6. Edge Cases section -- add these rows to the existing table:**
- `| Semantic Scholar MCP unavailable | Skip Step 2.5; insert [CITATION NEEDED] markers in outline; proceed to Step 4 |`
- `| Zero search results for an H2 | Display warning with ⚠ prefix; mark H2 as [CITATION NEEDED]; continue to next H2 |`
- `| MCP call fails mid-batch | Mark that specific H2 as [CITATION NEEDED]; continue with remaining H2 subsections |`
- `| Springer papers with empty abstracts | Call get-paper-abstract; if still empty, display "Abstract not available" in ref card; still include paper |`

**7. Fallbacks section -- add this row to the existing table:**
- `| Semantic Scholar MCP unavailable | Skip Step 2.5 entirely; add [CITATION NEEDED] after each H2 description; warn user; proceed to H3 generation |`

**8. Anti-patterns to avoid (do NOT include these in the Skill):**
- Do NOT invoke literature-skill as a sub-Skill. Call MCP tools directly within Step 2.5
- Do NOT save refs to the project's `references/` directory. Use `{repo_path}/.paper-refs/`
- Do NOT present numbered selection lists per H2. Batch auto-selects by relevance
- Do NOT supplement missing MCP fields from Claude's training data

**Line budget guidance:** CONTEXT.md explicitly allows exceeding the 300-line convention. Target ~350 lines. If draft exceeds ~380 lines, condense the ref card format specification by defining it once and referencing it (do not duplicate the card format in both Workflow and Output Contract).
  </action>

  <verify>
    <automated>
echo "=== Verify Step 2.5 exists ===" && grep -c "Step 2.5: Literature Collection" ".claude/skills/repo-to-paper-skill/SKILL.md" && echo "=== Verify pre-flight check ===" && grep -c "papers-search-basic.*test.*limit.*1" ".claude/skills/repo-to-paper-skill/SKILL.md" && echo "=== Verify CITATION NEEDED fallback ===" && grep -c "CITATION NEEDED" ".claude/skills/repo-to-paper-skill/SKILL.md" && echo "=== Verify .paper-refs directory ===" && grep -c "\.paper-refs/" ".claude/skills/repo-to-paper-skill/SKILL.md" && echo "=== Verify BibTeX anti-hallucination ===" && grep -c "MUST come from MCP" ".claude/skills/repo-to-paper-skill/SKILL.md" && echo "=== Verify External MCP in tools ===" && grep -c "External MCP" ".claude/skills/repo-to-paper-skill/SKILL.md" && echo "=== Verify literature_refs in output_contract ===" && grep -c "literature_refs" ".claude/skills/repo-to-paper-skill/SKILL.md" && echo "=== Verify get-paper-abstract ===" && grep -c "get-paper-abstract" ".claude/skills/repo-to-paper-skill/SKILL.md" && echo "=== Verify citation key format ===" && grep -c "firstAuthor" ".claude/skills/repo-to-paper-skill/SKILL.md" && echo "=== Line count ===" && wc -l ".claude/skills/repo-to-paper-skill/SKILL.md"
    </automated>
  </verify>

  <acceptance_criteria>
    - `.claude/skills/repo-to-paper-skill/SKILL.md` contains `### Step 2.5: Literature Collection` heading
    - Frontmatter `tools` list includes `External MCP`
    - Frontmatter `output_contract` list includes `literature_refs`
    - Step 2.5 contains pre-flight check calling `papers-search-basic` with `{"query": "test", "limit": 1}`
    - Step 2.5 contains fallback: skip entire step and insert `[CITATION NEEDED]` when MCP unavailable
    - Step 2.5 references `mcp__semantic-scholar__get-paper-abstract` for empty abstracts
    - Step 2.5 specifies ref file directory as `{repo_path}/.paper-refs/`
    - Step 2.5 includes BibTeX anti-hallucination rule: "All BibTeX fields MUST come from MCP-returned data"
    - Step 2.5 includes citation key format `firstAuthorLastnameLowercaseYYYYfirstKeyword`
    - Step 2.5 includes progress display format with checkmark/warning prefixes
    - Step 2.5 includes summary table format with Section, Subsection, Refs, Top Reference columns
    - Edge Cases table includes rows for MCP unavailable, zero results, mid-batch failure, Springer abstracts
    - Fallbacks table includes row for Semantic Scholar MCP unavailable
    - Total file line count is between 330 and 400 lines
    - Step order in Workflow is: Step 1, Step 2, Step 3, Step 2.5, Step 4 (decimal numbering preserved)
  </acceptance_criteria>

  <done>
    repo-to-paper-skill SKILL.md contains a complete Step 2.5 Literature Collection that:
    (1) runs pre-flight MCP check and skips gracefully with [CITATION NEEDED] on failure,
    (2) derives 2-5 keyword queries from H1+H2 titles,
    (3) batch-searches Semantic Scholar and auto-selects 5-10 refs per H2 by relevance,
    (4) writes per-H1-section Markdown ref files to {repo_path}/.paper-refs/ with card format,
    (5) displays progress per H2 and summary table before user confirms to proceed to H3
  </done>
</task>

</tasks>

<verification>
1. `grep "Step 2.5: Literature Collection" .claude/skills/repo-to-paper-skill/SKILL.md` returns a match
2. `grep "External MCP" .claude/skills/repo-to-paper-skill/SKILL.md` returns a match in the frontmatter tools list
3. `grep "literature_refs" .claude/skills/repo-to-paper-skill/SKILL.md` returns a match in the frontmatter output_contract
4. `grep "CITATION NEEDED" .claude/skills/repo-to-paper-skill/SKILL.md` returns matches (fallback behavior)
5. `grep ".paper-refs/" .claude/skills/repo-to-paper-skill/SKILL.md` returns matches (ref file directory)
6. `grep "MUST come from MCP" .claude/skills/repo-to-paper-skill/SKILL.md` returns a match (anti-hallucination)
7. `wc -l .claude/skills/repo-to-paper-skill/SKILL.md` returns a count between 330 and 400
8. The Step ordering in the Workflow section is: Step 1 -> Step 2 -> Step 3 -> Step 2.5 -> Step 4
</verification>

<success_criteria>
- REPO-04 is addressed: the repo-to-paper-skill now includes Semantic Scholar MCP batch search at H2 stage with ref file output
- SKILL.md is a complete, self-contained Skill document that an executor can follow without additional context
- MCP fallback is graceful: [CITATION NEEDED] placeholders, not hard failure
- Ref file format matches the locked Markdown card specification from CONTEXT.md
- File stays within acceptable line budget (~330-400 lines, explicitly allowed to exceed 300)
</success_criteria>

<output>
After completion, create `.planning/phases/15-literature-integration/15-01-SUMMARY.md`
</output>
